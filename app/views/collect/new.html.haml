%h1 Guess I better build a form here...
= form_for @proposal, url: new_proposal_path(@event) do |f|
  - @event.blinds.each do |blind|
    %section{ class: "blind" }
      %h2!= blind.title
      %p!= blind.info
      = f.fields_for blind.responses do |r|
        - blind.responses.each do |response|
          .question
            = r.label :value, "#{response.question.position} #{response.question.label}"
            = r.text_field :value
