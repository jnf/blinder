%h1 Guess I better build a form here...
= form_for @proposal, url: new_proposal_path(@event) do |f|
  - @event.blinds.each do |blind|
    %section{ class: "blind" }
      %h2!= blind.title
      %p!= blind.info
      = render blind.responses
  = f.submit "Submit Proposal"
